let INDICE = 13;
let SOMA = 0;
let K = 0;

while (K < INDICE) {
    K = K + 1;
    SOMA = SOMA + K;
}

// Ao final do loop, o valor de SOMA será 91, pois é a soma dos números de 1 a 13.
console.log("O valor final de SOMA é:", SOMA); // Output esperado: "O valor final de SOMA é: 91"
